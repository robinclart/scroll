<div id="<%= dom_id(component) %>"
     class="TextComponent <%= text_component.style %>"
     data-selectable="Selected"
     data-commands="#<%= dom_id(component) %>_commands"
     data-reload="<%= url_for component %>">

  <%= markdown text_component.content.presence || "no text" %>

</div>
